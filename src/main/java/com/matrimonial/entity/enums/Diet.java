package com.matrimonial.entity.enums;

/**
 * ENUM: Diet
 *
 * Shared by Profile (user's own diet) and Expectation (preferred partner diet).
 * Placed in enums package to avoid duplication across entities.
 */
public enum Diet {
    VEG,
    NON_VEG,
    VEGAN
}
